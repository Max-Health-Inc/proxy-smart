import type { EnrichContext } from './enrichContext.js';
/** Core resource handlers: generic passes + Patient, Condition, Observation, etc. */
export declare function enrichCoreHandlers(ctx: EnrichContext): void;
