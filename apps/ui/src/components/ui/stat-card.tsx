/**
 * Re-export from shared-ui — the admin-ui local copy was identical.
 * Keeping this barrel file so existing `@/components/ui/stat-card` imports work.
 */
export { StatCard, colorMap, type StatCardProps, type StatCardColor } from '@proxy-smart/shared-ui';
