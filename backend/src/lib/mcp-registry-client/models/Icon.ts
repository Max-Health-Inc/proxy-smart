/* tslint:disable */
/* eslint-disable */
/**
 * Official MCP Registry
 * A community driven registry service for Model Context Protocol (MCP) servers.

[GitHub repository](https://github.com/modelcontextprotocol/registry) | [Documentation](https://github.com/modelcontextprotocol/registry/tree/main/docs)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by generate-ts-fetch-client.py
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';

/**
 * @export
 * @interface Icon
 */
export interface Icon {
    /**
     * Optional MIME type override if the source MIME type is missing or generic. Must be one of: image/png, image/jpeg, image/jpg, image/svg+xml, image/webp.
     * @type {IconMimeTypeEnum}
     * @memberof Icon
     */
    mimeType?: IconMimeTypeEnum;
    /**
     * Optional array of strings that specify sizes at which the icon can be used. Each string should be in WxH format (e.g., '48x48', '96x96') or 'any' for scalable formats like SVG. If not provided, the client should assume that the icon can be used at any size.
     * @type {Array<string> | null}
     * @memberof Icon
     */
    sizes?: Array<string> | null;
    /**
     * A standard URI pointing to an icon resource. Must be an HTTPS URL. Consumers SHOULD take steps to ensure URLs serving icons are from the same domain as the server or a trusted domain. Consumers SHOULD take appropriate precautions when consuming SVGs as they can contain executable JavaScript.
     * @type {string}
     * @memberof Icon
     */
    src: string;
    /**
     * Optional specifier for the theme this icon is designed for. 'light' indicates the icon is designed to be used with a light background, and 'dark' indicates the icon is designed to be used with a dark background. If not provided, the client should assume the icon can be used with any theme.
     * @type {IconThemeEnum}
     * @memberof Icon
     */
    theme?: IconThemeEnum;
}

/**
 * @export
 */
export const IconMimeTypeEnum = {
    Image_png: 'image/png',
    Image_jpeg: 'image/jpeg',
    Image_jpg: 'image/jpg',
    Image_svg_xml: 'image/svg+xml',
    Image_webp: 'image/webp',
} as const;
export type IconMimeTypeEnum = typeof IconMimeTypeEnum[keyof typeof IconMimeTypeEnum];

/**
 * @export
 */
export const IconThemeEnum = {
    Light: 'light',
    Dark: 'dark',
} as const;
export type IconThemeEnum = typeof IconThemeEnum[keyof typeof IconThemeEnum];

/**
 * Check if a given object implements the Icon interface.
 */
export function instanceOfIcon(value: object): value is Icon {
    if (!('src' in value) || value['src'] === undefined) return false;
    return true;
}

export function IconFromJSON(json: any): Icon {
    return IconFromJSONTyped(json, false);
}

export function IconFromJSONTyped(json: any, ignoreDiscriminator: boolean): Icon {
    if (json == null) {
        return json;
    }
    return {
        
        'mimeType': json['mimeType'] == null ? undefined : json['mimeType'],
        'sizes': json['sizes'] == null ? undefined : json['sizes'],
        'src': json['src'],
        'theme': json['theme'] == null ? undefined : json['theme'],
    };
}

export function IconToJSON(json: any): Icon {
    return IconToJSONTyped(json, false);
}

export function IconToJSONTyped(value?: Icon | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'mimeType': value['mimeType'],
        'sizes': value['sizes'],
        'src': value['src'],
        'theme': value['theme'],
    };
}
