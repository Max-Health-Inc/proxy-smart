/* tslint:disable */
/* eslint-disable */
/**
 * Official MCP Registry
 * A community driven registry service for Model Context Protocol (MCP) servers.

[GitHub repository](https://github.com/modelcontextprotocol/registry) | [Documentation](https://github.com/modelcontextprotocol/registry/tree/main/docs)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by generate-ts-fetch-client.py
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ServerJSON,
  ServerResponse,
} from '../models/index';
import {
    ServerJSONFromJSON,
    ServerJSONToJSON,
    ServerResponseFromJSON,
    ServerResponseToJSON,
} from '../models/index';

export interface PublishServerV01Request {
    authorization: string;
    serverJSON: ServerJSON;
}

export interface PublishServerV0Request {
    authorization: string;
    serverJSON: ServerJSON;
}

/**
 * 
 */
export class PublishApi extends runtime.BaseAPI {

    /**
     * Publish a new MCP server to the registry or update an existing one
     * Publish MCP server
     */
    async publishServerV01Raw(requestParameters: PublishServerV01Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServerResponse>> {
        if (requestParameters['authorization'] == null) {
            throw new runtime.RequiredError(
                'authorization',
                'Required parameter "authorization" was null or undefined when calling publishServerV01().'
            );
        }

        if (requestParameters['serverJSON'] == null) {
            throw new runtime.RequiredError(
                'serverJSON',
                'Required parameter "serverJSON" was null or undefined when calling publishServerV01().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        if (requestParameters['authorization'] != null) {
            headerParameters['Authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0.1/publish`;

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServerJSONToJSON(requestParameters['serverJSON']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServerResponseFromJSON(jsonValue));
    }

    /**
     * Publish a new MCP server to the registry or update an existing one
     * Publish MCP server
     */
    async publishServerV01(requestParameters: PublishServerV01Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServerResponse> {
        const response = await this.publishServerV01Raw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publish a new MCP server to the registry or update an existing one
     * Publish MCP server
     */
    async publishServerV0Raw(requestParameters: PublishServerV0Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServerResponse>> {
        if (requestParameters['authorization'] == null) {
            throw new runtime.RequiredError(
                'authorization',
                'Required parameter "authorization" was null or undefined when calling publishServerV0().'
            );
        }

        if (requestParameters['serverJSON'] == null) {
            throw new runtime.RequiredError(
                'serverJSON',
                'Required parameter "serverJSON" was null or undefined when calling publishServerV0().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        if (requestParameters['authorization'] != null) {
            headerParameters['Authorization'] = String(requestParameters['authorization']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v0/publish`;

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServerJSONToJSON(requestParameters['serverJSON']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServerResponseFromJSON(jsonValue));
    }

    /**
     * Publish a new MCP server to the registry or update an existing one
     * Publish MCP server
     */
    async publishServerV0(requestParameters: PublishServerV0Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServerResponse> {
        const response = await this.publishServerV0Raw(requestParameters, initOverrides);
        return await response.value();
    }

}
