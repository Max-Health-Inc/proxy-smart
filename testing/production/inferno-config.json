{
  "test_stage": "production",
  "description": "Production consent-enforcement probe - authenticated, but mutates nothing (see mode).",
  "target": "deployed",
  "mode": "enforcement",
  "deployed_url": "https://api.proxy-smart.com",
  "fhir_server": {
    "name": "Proxy Smart FHIR Server (AWS Production)",
    "url": "https://api.proxy-smart.com/proxy-smart-backend/hapi-fhir-server/R4"
  },
  "keycloak": {
    "url": "https://auth.proxy-smart.com",
    "realm": "proxy-smart"
  },
  "backend": {
    "url": "https://api.proxy-smart.com"
  },
  "client": {
    "client_id": "ci-enforcement-probe"
  },
  "test_user": {
    "username": "ci-enforcement-probe"
  },
  "probe": {
    "comment": "Patient id the probe requests. Deliberately never created: consent is evaluated before the request is proxied upstream, so an id with no Consent resource must be denied. If enforcement regresses to audit-only the request falls through and returns 404 instead of 403, failing the probe.",
    "patient_id": "ci-enforcement-probe-no-consent"
  },
  "test_settings": {
    "timeout": 120,
    "retries": 10,
    "validate_ssl": true
  }
}
